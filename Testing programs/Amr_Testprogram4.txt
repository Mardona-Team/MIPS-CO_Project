Program #4: comparing &multply the least by 4
-----------

C-code:
       int X,y;
       if(x<y)
       x=*4;
       elseif(y<x)
       y=*4;
       else
       {x=*4;y=*4}

x-->$s0  y-->$s1

Assembly:
         slt $t0,$s1,$s0
         beq $t0,$zero,L1        
         sll $s1,$s1,2
         jal Exit
      
      l1:slt $t0,$s0,$s1
         beq $t0,$zero,l2
         sll $s0,$s1,2
         jal Exit
 
      l2:sll $s0,$s1,2
         sll $s0,$s1,2
         jal Exit
    
    Exit: